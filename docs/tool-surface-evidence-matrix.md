# Tool surface evidence matrix — Write, Delete, Move, WebFetch

**Date:** 2026-08-15
**Scope:** ADR-0016 (whole-file write, delete, move) and ADR-0017 (web reads)
**Rule:** no PASS without named, executable evidence.

The tool surface has never had a matrix, which is why the alpha.6 retrospective
could say "zero tool defects" without being able to say what that was evidence
of. This is the first one. It covers what the four new tools _enforce_; it does
not yet cover whether they are **useful** — no task-success rate, no step counts,
no failure attribution — and that gap is stated in the last section rather than
being left for a reader to notice.

| Requirement                                                | Status     | Evidence                                                                                                                                           | Notes                                                                                                                                                                                       |
| ---------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Create through Write needs no receipt                      | PASS       | test:creates a file that does not exist, with no receipt                                                                                           | there is no content to lose                                                                                                                                                                 |
| Overwrite without a receipt is refused                     | PASS       | test:refuses to overwrite an existing file without a receipt                                                                                       | file bytes asserted unchanged                                                                                                                                                               |
| Overwrite from a partial read is refused                   | PASS       | test:refuses to overwrite a file that was only read in part                                                                                        | `INSUFFICIENT_READ_COVERAGE`                                                                                                                                                                |
| Overwrite after a full read applies, with a real diff      | PASS       | test:overwrites after a full read, and records the diff                                                                                            | journal entry kind `overwrite`                                                                                                                                                              |
| A spent receipt cannot be replayed                         | PASS       | test:a spent receipt cannot be reused                                                                                                              | `STALE_FILE`                                                                                                                                                                                |
| Delete needs a full-coverage receipt                       | PASS       | test:requires a receipt for a file                                                                                                                 | the file survives the refusal                                                                                                                                                               |
| Delete asks, and the journal can undo it                   | PASS       | test:removes a file that was read in full, once approved                                                                                           | diff carries the whole file                                                                                                                                                                 |
| A denied deletion changes nothing                          | PASS       | test:a denied deletion leaves the file alone                                                                                                       | `TOOL_DENIED`                                                                                                                                                                               |
| Recursive deletion is not implemented                      | PASS       | test:removes an empty directory, and refuses one with contents                                                                                     | ADR-0016 consequence, asserted rather than assumed                                                                                                                                          |
| Move never overwrites                                      | PASS       | test:never overwrites an existing destination                                                                                                      | checked at resolve and in the backend                                                                                                                                                       |
| Move invalidates receipts for both paths                   | PASS       | test:renames a file and invalidates the receipts for both paths                                                                                    |                                                                                                                                                                                             |
| Protected paths cannot be deleted or overwritten           | PASS       | test:Delete on a .env is hard denied, whatever the user would approve, test:Write cannot overwrite a secret file either                            | prompter never reached                                                                                                                                                                      |
| A rename cannot reach a protected path in either direction | PASS       | test:Move cannot use a rename to reach a protected destination                                                                                     | onto `.env`, and `.env` away                                                                                                                                                                |
| A symlink does not launder a deletion                      | PASS       | test:a symlink into a credential directory does not launder a delete                                                                               | canonicalisation, then `PROTECTED_PATH`                                                                                                                                                     |
| The git database is not edited by hand                     | PASS       | test:Delete refuses a path inside .git                                                                                                             | tool-level refusal, not a policy one                                                                                                                                                        |
| The workspace root cannot be deleted or moved              | PASS       | test:the workspace root cannot be deleted or moved                                                                                                 |                                                                                                                                                                                             |
| Read-only profiles have no destructive surface             | PASS       | test:Delete, Write and Move are all denied under read-only                                                                                         | denial, not a prompt                                                                                                                                                                        |
| Deleting is a different approval from writing              | PASS       | test:approving a write for a file does not approve deleting it                                                                                     | `file.delete:` subject key                                                                                                                                                                  |
| remove/rename behave alike on every backend                | PASS       | test:Write then Delete removes the file, on every backend, test:Move renames a file, on every backend                                              | local + container (Docker Desktop) tiers, both run 2026-08-15                                                                                                                               |
| the SSH backend removes and renames the same way           | PASS       | test:removes a remote file, and an empty directory, but not a full one, test:renames a remote file, and refuses to clobber an existing destination | live against `linux-vm`, 53/53, 2026-08-15                                                                                                                                                  |
| WebFetch is absent when no web host is configured          | PASS       | test:no [egress] web means no WebFetch in the catalogue                                                                                            | also asserted in the delegation catalogue test                                                                                                                                              |
| An unconfigured host is refused without prompting          | PASS       | test:an unconfigured host is refused, and never prompts                                                                                            | transport never reached                                                                                                                                                                     |
| Non-global addresses are refused, including odd spellings  | PASS       | test:the cloud metadata address is refused even if it is spelled unusually                                                                         | `169.254.169.254`, loopback, `0x7f.0.0.1`                                                                                                                                                   |
| Credentialed and non-http URLs are refused                 | PASS       | test:non-http schemes and credentialed URLs are refused                                                                                            |                                                                                                                                                                                             |
| A fetch asks per host and goes out as a plain GET          | PASS       | test:returns the page as text, labelled as untrusted                                                                                               | subject key `network.connect:web:scoped:…`                                                                                                                                                  |
| A denied fetch sends nothing                               | PASS       | test:a denied approval sends nothing at all                                                                                                        |                                                                                                                                                                                             |
| Redirects are reported, not followed                       | PASS       | test:a redirect is reported, not followed                                                                                                          | one transport request, to the original URL                                                                                                                                                  |
| Non-text responses are refused                             | PASS       | test:an image response is refused by content type                                                                                                  |                                                                                                                                                                                             |
| Script and style contents never reach the model            | PASS       | test:drops script and style contents entirely, test:drops an unterminated script, rather than spilling it                                          | including a truncated response                                                                                                                                                              |
| A credential in a page is redacted before the context      | PASS       | test:a credential in the page is redacted before it reaches the model                                                                              | canary absent from the event log too                                                                                                                                                        |
| An oversized body is cut to the budget                     | PASS       | test:an oversized body is cut to the requested budget                                                                                              |                                                                                                                                                                                             |
| The gate is the last word, whatever policy said            | PASS       | test:a host approved by policy but absent from the gate allowlist is still blocked                                                                 | gate narrowed under the tool                                                                                                                                                                |
| Fetched content cannot inject instructions                 | NOT TESTED |                                                                                                                                                    | not a technical control; see `docs/threat-model.md` — the label reduces the surface, nothing closes it [open:B2]                                                                            |
| Tool usability: task success rate, step counts             | NOT TESTED |                                                                                                                                                    | no harness existed at the time; this is the gap that motivated the round. **Closed by alpha.7 §B** — the friction table in the eval runner, and three two-arm experiments since [closed:C3] |

## Model provenance

**Every behavioural number in this matrix was measured on one model:
`deepseek-chat` (DeepSeek, `openai-chat` protocol), N=5 per cell.**

This is the matrix where that caveat bites hardest, because its central finding is
about _how a model calls tools_: adding `Write` made `Edit` harder to call, and the
model dropped Edit's required `mode` in 10 of 20 calls. Whether that is a fact
about this tool surface or a fact about this model is precisely the question one
model cannot answer.

alpha.8 §20 re-ran the tool-utility experiment against a second model under §21's
fairness rules — same fixtures, same prompts, same `fixtureVersion`, same N, no
per-model prompt tuning. The result is reported beside this one in
`docs/alpha8-evidence-matrix.md`, never averaged into it.

Structural rows (a tool exists, a capability is refused, an approval is required)
are model-independent and hold for any model.

## What this matrix does not claim

Every row above is about a boundary. None of them says the tools are _good_ — a
`Write` that always refuses would pass most of this table. Measuring that needs a
usability harness (task success rate, steps per task, failure attribution by
layer), which does not exist and is the next thing worth building.
